  resources:
    volumes:
      landing:
        catalog_name: inf_bronze_${var.env}
        schema_name: ${resources.schemas.bronze.name}
        name: landing
        lifecycle:
          prevent_destroy: false