resources:
  pipelines:
    pipleine:
      name: pipleine
      libraries:
        - glob:
            include: ../src/pipleine/transformations/**
      serverless: true
      catalog: dev_bronze
      schema: ${workspace.current_user.short_name}
      root_path: ../src/pipleine
