  resources:
    volumes:
      landing:
        catalog_name: ${bundle.target}_inf
        schema_name: ${resources.schemas.resorts.name}
        name: landing
        grants:
          - principal: inf-developers
            privileges:
              - READ_VOLUME
              - WRITE_VOLUME
        lifecycle:
          prevent_destroy: false