  resources:
    volumes:
      landing:
        catalog_name: ${bundle.target}_inf
        schema_name: ${resources.schemas.bronze.name}
        name: landing
        lifecycle:
          prevent_destroy: false